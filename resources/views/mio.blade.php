<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>anal</title>
</head>
<body>

    @if($product)
        @foreach ($product as $p)
        <p>{{ $p->name }}</p>
        @endforeach
    @endif
</body>
</html>
